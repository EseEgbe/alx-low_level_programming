#include "main.h"

/**
 * _abs - Entry point
 *
 * @int: Inputed integer.
 *
 * Description: computes the absolute value.
 *
 * Return: absolute value
 */

int _abs(int)
{
	int = i;

	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
